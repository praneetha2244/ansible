- name: loops
  hosts: local
  connection: local
  become: yes
  tasks:
    - name: print names
      ansible.builtin.debug:
        msg: "Hello {{ item }}"
      loop:
        - nginx
        - mysql
        - zip
