- name: conditions
  hosts: local
  connection: local
  vars:
    number: 5   # make sure this is an integer
  tasks:
  - name: check number is less than 10 or not
    ansible.builtin.debug:
      msg: "Given number {{ number }} is less than 10"
    when: number | int < 10
